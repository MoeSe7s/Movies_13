name: movies_project
description: "A new Flutter project."

publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any # تأكد من استخدام إصدار متوافق مع Flutter 3
  provider: ^6.1.2
  animated_toggle_switch: ^0.6.0
  flutter_gen: ^5.3.2

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

